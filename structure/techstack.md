Next.js 15 (App Router) → SSR, SEO, file-based routing, RSC
TypeScript → Type safety across the entire project
Tailwind CSS → Utility-first, fast, consistent
Framer Motion → Page transitions + scroll animations
PostgreSQL (appwrite) → Relational DB for reservations, menu, reviews
Resend → Email notifications (reservation confirmations)
Cloudinary → Image hosting + optimization
Vercel → Deployment + Edge functions
